# Code-Edit
Программа по редактированию кода
____

## Пример ввода:

```c
    #include <stdio.h>
int col (int n, int y, int x){
  /*Привет
  я рекурсивная функция*/
  if ( n/10 == 0){
    return n;}else{
return n%10 + col (n/10,a,b);
  }for ( ; ; )//Вечный цикл
  {
    y = sum(x, 5);}
}
int SuMMa(int x, int z)
{
  int y;
  y=x+z;
  return(y);
}
int main ()
{
  int p;
  int i,j,k = 0;
char zebu;
char pekatka /*тут щас будет сканф*/;
  scanf("%c", zebu);//это не должно работать
  scanf("%c", &pekatka);
  //
  printf("%c",'zebu');
  printf("%c",'pekatka');
  //
  /*tgkfecigbvtun2c]  -[r,DS CL,Dsnuybth,owmcjbnkzd.*/
int a = 15; int b = a;
int z = 0; int n;
scanf("%d", &n);/*Тут
  вызывается рекурсивная функция,
  но я не помню,
  что она делает*/
  printf("%d\n",col(n,a,b));
  z = col(n);
           for /*цикл в цикле в цикле*/(i = 0; i <= a; i++){
  for(j = 20; j>= b - 10; j--){
  while(k < 10)
  k++;
}
  }
  printf("%d",k)/*Вечный цикл*/;
  do 
  {
    printf("%c",'*');
  }
    while (1);
  k = 0/*Вечный цикл*/;
for ( int i = 0; i<= k ; i--) { i ++ }
               a = 15;
  b = 4;
  /*тут вызывается
                    вторая
функция*/
 int sum = SuMMa(a,b);
  printf("%d", p+3)/*Не инициализированный p*/;
int num;
scanf("%d", &num); int rec = num+2;
return 0;
}
```

____
## Пример вывода:

```c
#include <stdio.h>

int col (int n, int y, int x){
    //Привет
    //я рекурсивная функция
    if ( n/10 == 0){
        return n;
    }
    else{
        return n%10 + col (n/10,a,b);
    }
    for ( ; ; ){ //Вечный цикл
        y = sum(x, 5);
    }
}
int SuMMa(int x, int z){
  int y;
  y = x + z;
  return(y);
}

int main(){
    int p;
    int i,j,k = 0;
    char zebu;
    char pekatka; //тут щас будет сканф
    scanf("%c", zebu); //это не должно работать
    scanf("%c", &pekatka);
  //
    printf("%c",'zebu');
    printf("%c",'pekatka');
  //
    //tgkfecigbvtun2c]  -[r,DS CL,Dsnuybth,owmcjbnkzd.
    int a = 15; 
    int b = a;
    int z = 0; 
    int n;
    scanf("%d", &n);
    //Тут
    //вызывается рекурсивная функция,
    //но я не помню,
    //что она делает
    printf("%d\n",col(n,a,b));
    z = col(n);
    for(i = 0; i <= a; i++){ //цикл в цикле в цикле
        for(j = 20; j >= b - 10; j--){
            while(k < 10){
                k++;
            }
        }
    }
    printf("%d",k); //Вечный цикл
    do{
        printf("%c",'*');
    }
    while (1);
    k = 0; //Вечный цикл
    for ( int i = 0; i <= k ; i--){ 
        i ++; 
    }
    a = 15;
    b = 4;
    //тут вызывается
    //вторая
    //функция
    int sum = SuMMa(a,b);
    printf("%d", p+3); //Не инициализированный p
    int num;
    scanf("%d", &num); 
    int rec = num + 2;
    return 0;
}
```
---
